# Panic Marathon 개발 기획서

> 최종 갱신: 2026-07-27
> 문서 상태: 현재 구현·확정 규칙·다음 작업을 함께 기록하는 인수인계 기준 문서다. 새 세션에서는 이 문서의 **현재 구현 상태**, **남은 과제**, **개발 환경**을 먼저 확인한다.

## 1. 게임 한 줄 소개

**Panic Marathon**은 탑다운 픽셀 아레나에서 2~6명의 플레이어가 함정이 있는 트랙을 한 바퀴 돌며, 랜덤 스킬로 서로의 주행을 방해하는 병맛 경쟁 레이싱 게임이다.

## 2. 목표와 설계 원칙

### 대회용 목표

- 첫 플레이 후 30초 안에 이동, 함정, 스킬의 재미를 이해하게 한다.
- 기본 5랩 경기 기준으로, 한 판의 목표 시간을 플레이테스트 후 확정한다. 5랩이 지나치게 길면 방장이 랩 수를 낮춰 설정할 수 있다.
- 실력만으로 고정되지 않되, 스킬 조준·타이밍·위치 선정으로 역전할 여지를 만든다.
- 픽셀 그래픽, 과장된 넉백, 투명한 규칙으로 보기만 해도 상황을 이해하게 한다.

### 설계 원칙

1. **달리는 재미가 우선이다.** 스킬은 멈추게 하기보다 위치를 흔들고 선택을 강요한다.
2. **억까도 판의 재료다.** 강한 견제가 일어나면 다른 플레이어가 가해자를 함께 막거나 처치할 수 있어야 한다. 수면·구덩이·넉백의 원인은 시각적으로 명확하게 보여 준다.
3. **한 번에 읽혀야 한다.** 함정, 투사체, 쿨타임, 상태 이상을 색과 픽셀 효과로 구분한다.
4. **랜덤은 시작점이고, 조준과 운영이 실력이다.** 스킬이 랜덤으로 지급되어도 사용 결과는 플레이어가 만든다.

## 3. 현재 구현 상태

### 구현됨

- 픽셀 스타일 대기실과 탑다운 카메라
- 실제 플레이어 스프라이트를 재사용한 타이틀 루프: 왼쪽 픽셀 로고·참여/생성 메뉴, 오른쪽 운동장 질주·총격·분신 난장판
- 브라우저 전체화면 버튼과 화면을 최대한 채우는 레이스 화면
- 게임 내부 로비 UI: 방 참여·방 생성·규칙 설정 패널
- 1랩 운동장 트랙, 체크포인트 3개, 시작선
- 회전봉, 랜덤 구덩이, 역방향 점프대
- 구덩이 축소 낙하 후 체크포인트 부활, 점프대 체공 넉백
- 마우스 조준선·크로스헤어·조준 펄스
- 2~6인·1~999랩·스킬 풀을 설정하는 실제 방 생성 화면
- Socket.IO 기반 실시간 멀티플레이: 방 생성·참가·방장 시작·퇴장·자동 방장 위임
- 서버 메모리 기반 방/플레이어/분신 상태 관리와 원격 플레이어 보간 렌더링
- 플레이어별 의상 색상, 머리 위 이름·스킬·쿨타임/횟수·라이프·탄창 표시
- 좌측 상단 레이스 현황판의 원격 플레이어 진행도 표시
- 스폰/체크포인트의 단일 랜덤 스킬 지급, 우클릭 스킬 사용
- 좌클릭 3발 기본 총, 머리 위 5칸 라이프, 0 라이프 체크포인트 부활
- 구덩이·체크포인트·라이프 소진 후 체크포인트 부활 시 3발 탄창 회복
- 기본 총, 밀치기, 돌진, 질주, 그랩, 분신, 슬로우탄, 수면총의 멀티플레이 전투 이벤트와 서버 판정
- 체크포인트 순서·랩·1등 완주·체크포인트 탄창 회복·랜덤 스킬 재지급의 서버 권한 판정
- 서버 기준 랜덤 구덩이 경고/개방 주기와 구덩이 낙하·점프대 체공 상태 동기화
- 원격 플레이어의 구덩이 축소 낙하, 점프대 체공, 밀치기 곡선 이동 애니메이션
- 수면총·슬로우탄의 서버 투사체 이동/충돌 판정과 슬로우 폭발·지속 오라
- 밀치기 도착점의 서버 트랙 경계 보정으로 경기장 밖 고립 방지
- `waiting → running → finished` 경기 단계, 전체 결과 순위, 1시간 제한과 방장 재대결
- 낙하·체공·그랩·밀치기·수면 상태의 서버 공통 우선순위와 행동 잠금
- 점프대의 서버 확정 도착 좌표와 클라이언트 체공 이동 애니메이션
- 게임에서 나갈 때 구형 대기실이 아닌 현재 타이틀 화면으로 복귀
- 능동 스킬 7종과 자동 스킬 3종의 멀티플레이 버전
- 고정 플레이어 ID와 재접속 토큰을 사용한 30초 연결 복구, 상태 보존과 유예 만료 후 방장 위임
- 안전 난간이 있는 말썽 운동장, 트랙 이탈 시 추락하는 우주 정거장, 직선 오르막의 우당탕 산맥
- Web Audio 기반 기본 총·스킬·피격·체크포인트·함정·완주 효과음, 64스텝 연속 칩튠 BGM과 음소거 설정
- 입력, 오디오, 맵 콘텐츠, 월드·러너 렌더러 및 서버 방 상태의 모듈 분리

### 아직 구현되지 않음

- 공개 방 목록
- 영속 계정·전적·방 목록과 다중 서버 상태 공유
- 추가 접근성 옵션과 외부 제작 정식 캐릭터 애니메이션

### 현재 확인된 품질 이슈

- 점프대 통합 테스트의 고정 이동 경로가 무작위 회전 장애물과 충돌하던 문제를 해결했고, 무작위 배치를 바꿔가며 반복 검증했다.
- `속도 30%`와 `5배 거대화`의 고정 이동 속도는 공통 규칙에서 계산하며 질주·슬로우와 중첩되지 않는다.
- `npm run verify`의 타입 검사, 자동 테스트 35개, 프로덕션 빌드와 서버 구문 검사가 모두 통과한다.
- 자동 검증은 연결·서버 판정·배포 회귀를 다루지만, 스킬 재미·가독성·맵별 밸런스는 수동 2·4·6인 플레이테스트가 필요하다.

## 4. 핵심 경기 흐름

```text
대기실
  → 방 생성(랩 수·사용 스킬 설정) 또는 참가
  → 방장이 2명 이상일 때 경기 시작
  → 랜덤 스킬 1개 배정 / 경기 시작
  → 체크포인트 1 → 2 → 3 → 시작선 복귀
  → 체크포인트 통과 시 다음 랜덤 스킬 1개로 교체
  → 설정된 랩 수(기본 5랩) 완주 순위 확정
  → 결과 화면 → 재대결 또는 대기실
```

### 기본 규칙 초안

| 항목 | 제안 |
| --- | --- |
| 인원 | 2~6명 |
| 랩 수 | 방장 설정 가능, 기본 5랩 |
| 승리 조건 | 체크포인트를 순서대로 통과하고 설정된 랩 수를 가장 먼저 완주한 1명만 승리 |
| 경기 시간 | 기본 5랩의 목표 시간은 고정하지 않음. 장기전 방은 최대 1시간 시간 제한 적용 |
| 리스폰 | 마지막으로 통과한 체크포인트 |
| 기본 이동 속도 | 110 |
| 스킬 슬롯 | 항상 1개만 보유 |
| 랜덤 스킬 | 방장이 허용한 스킬 풀에서 스폰 시 1개 지급, 체크포인트 통과 시 새 스킬 1개로 교체 |
| 라이프 | 플레이어 머리 위에 5칸으로 표시 |
| 기본 공격 | 좌클릭 조준 총, 한 번에 최대 3발 발사 가능 |
| 스킬 사용 | 우클릭으로 현재 보유한 스킬 사용 |

### 레이스 현황판

경기 화면 왼쪽 상단에는 모든 플레이어의 진행 상태를 빠르게 읽을 수 있는 표를 항상 표시한다.

| 표시 항목 | 내용 |
| --- | --- |
| 플레이어 | 이름 또는 색상·캐릭터 아이콘 |
| 랩 | `현재 랩 / 설정 랩 수` — 기본값은 `0 / 5` |
| 체크포인트 | `CP 1`, `CP 2`, `CP 3`, `START` 중 현재 다음 목표 |
| 순위 | 현재 진행도 기준 임시 순위 |

플레이어 자신의 행은 강조 표시하고, 다른 플레이어의 라이프와 현재 스킬은 필요하면 축약 아이콘으로 표시한다.

### 방 생성 설정

방장은 경기 시작 전 아래 설정을 정한다. 설정은 경기 시작 시 잠기며, 참가자는 방 목록과 준비 화면에서 내용을 확인할 수 있다.

| 설정 | 기본값 | 규칙 |
| --- | --- | --- |
| 최대 인원 | 6명 | 최소 2명, 최대 6명 |
| 랩 수 | 5랩 | 방장이 `1~999` 범위의 정수를 숫자 입력창에 직접 입력 |
| 시간 제한 | 1시간 | 장기 방이 무한히 이어지지 않도록 최대 60분 후 경기 종료. 시간 종료 시 진행도와 다음 관문 거리로 순위를 확정 |
| 맵 | 말썽 운동장 | 방장이 말썽 운동장·우주 정거장·우당탕 산맥 중 선택 |
| 스킬 풀 | 전체 허용 | 방장이 경기에서 나올 수 있는 스킬을 켜고 끈다 |

스킬 풀에는 최소 3개 이상의 스킬이 활성화되어야 한다. 방장은 개별 스킬을 직접 켜고 끈다. 스킬 프리셋은 같은 스킬 묶음을 빠르게 선택하는 편의 기능일 뿐이므로, 초기 빌드에서는 제공하지 않고 필요해질 때 추가한다. 스폰과 체크포인트 재추첨은 활성화된 스킬 풀 안에서만 이뤄진다.

랩 수 입력은 빈 값·소수·문자·`1` 미만·`999` 초과를 허용하지 않는다. 기본값은 `5`이며, `999`랩처럼 긴 방은 의도적인 커스텀/지구력 모드로 허용한다.

## 5. 맵 기획

### 현재 트랙

- 상·하단 폭이 넓은 사각 1랩 트랙. 직선 구간을 초기 프로토타입보다 약 50% 길게 확장해 스킬 교전과 추격 구간을 확보
- 체크포인트는 시계 방향으로 배치
- 중앙은 경기장/연습 공간처럼 보이는 내야

### 함정 규칙

| 요소 | 의도 | 현재 동작 | 밸런스 확인 사항 |
| --- | --- | --- | --- |
| 회전봉 | 타이밍 회피 | 접촉 시 바깥 방향 넉백 | 한 번에 트랙 밖으로 너무 멀리 보내지 않는지 |
| 랜덤 구덩이 | 경로 긴장감 | 서버가 가까운 구덩이를 선택한 뒤 표식 없이 4초 동안 닫힌 상태를 유지하고 개방. 열린 상태는 4~6초이며, 빠지면 축소 낙하 후 체크포인트 리스폰 | 2~6인 실전에서 닫힘·개방 간격이 적절한지 |
| 역방향 점프대 | 병맛 역전 | 서버 판정으로 공중에 뜬 채 뒤로 날아가며 모든 클라이언트에 체공 모션 동기화 | 피할 수 있는지, 연속 밟기 방지 쿨타임이 적절한지 |

### 맵 구성

1. **말썽 운동장** *(현재 구현)*: 안전 난간, 회전봉, 랜덤 구덩이, 역방향 점프대가 있는 기본 트랙
2. **우주 정거장** *(현재 구현)*: 운동장과 같은 진행 동선을 익숙하게 유지하되 트랙 안팎을 우주 공간으로 바꿔, 경계를 넘거나 밀려나면 즉사 후 마지막 체크포인트에서 부활하는 고위험 트랙. 공중부양은 우주 공간을 건널 수 있지만 거대화는 추락한다.
3. **우당탕 산맥** *(현재 구현)*: 완만한 직선 오르막의 체크포인트 3개를 지나 정상에 도달하는 코스. 남은 랩이 있으면 산 아래 출발점으로 순간이동하며, 서버가 굴리는 낙석은 암벽 구조물에 닿으면 소멸한다. 기존 구덩이·점프대·회전봉은 등장하지 않는다.

동일한 코스에 색상만 바꿨던 공사장·옥상 프로토타입은 제거 상태를 유지한다. 우주 정거장은 추락 규칙으로, 우당탕 산맥은 직선 랩·순간이동·낙석 규칙으로 코스 자체를 차별화한다.

## 6. 스킬 기획

현재 스킬은 우클릭으로 사용하는 능동 스킬 7종과 지급 즉시 적용되는 자동 스킬 3종으로 구성한다. 실제 경기에서는 스폰 또는 체크포인트 통과 시 무작위 스킬 1개를 받고, 자동 스킬은 다음 스킬로 교체될 때까지 별도 입력 없이 유지된다.

| 번호 | 스킬 | 역할 | 현재 수치 | 경기 버전 검토 |
| --- | --- | --- | --- | --- |
| 1 | 밀치기 | 근거리 공간 장악 | 반경 72, 거리 56, 0.32초 넉백, 쿨타임 2.6초 | 서버가 트랙 내부의 가장 먼 안전 지점으로 도착점을 제한. 실제 2~6인에서 거리를 재검토 |
| 2 | 돌진 | 추격·회피 | 3회 충전, 소진 후 4.3초 | 충전 회복을 한 번에 할지, 1회씩 할지 결정 |
| 3 | 질주 | 안정적 주행 | 4.6초 지속, 9초 쿨타임 | 함정 면역은 주지 않음 |
| 4 | 그랩 | 선두 견제 | 사거리 250, 조준선 반경 12, 쿨타임 3.8초 | 로켓팔은 빗나가도 최대 사거리까지 왕복하고, 발사자·피격자는 왕복 0.52초 동안 이동 불가 |
| 5 | 분신 | 길막·심리전 | 플레이어별 최대 20개, 생성 간격 0.15초, 10초 유지 | 자신을 포함한 플레이어 이동만 막는다. 투사체·그랩·슬로우탄·수면총은 통과하며, 생성 시 겹친 상대는 바깥으로 밀어낸다 |
| 6 | 슬로우탄 | 범위 제어 | 반경 72, 3.6초 감속, 4.6초 쿨타임 | 서버 투사체 충돌 판정, 청록색 폭발과 피격자 지속 오라 구현. 다수 피격 시 가독성 확인 |
| 7 | 수면총 | 단일 대상 방해 | 2초 수면, 2초 쿨타임 | 연속 수면의 무한 방지 규칙 검토 |
| 8 | 공중부양 | 지상 장애물 회피 | 지급 즉시 자동 발동, 다음 체크포인트까지 유지 | 이동·기본 총은 허용하고 구덩이·점프대·회전봉 판정은 무시한다 |
| 9 | 속도 30% | 자동 역효과 | 지급 즉시 기본 이동 속도의 30%, 다음 체크포인트까지 유지 | 질주·슬로우와 중첩하지 않고 서버가 정확한 고정 배율로 검증한다 |
| 10 | 5배 거대화 | 이동 차단 | 지급 즉시 몸집 5배·속도 50%, 다음 체크포인트까지 유지 | 지상 장애물과 분신을 무시하며, 다른 플레이어는 서버 충돌 판정으로 거대한 몸을 통과할 수 없다 |

### 상태 이상 우선순위 초안

1. 구덩이 낙하: 모든 이동·스킬 취소
2. 수면: 이동·스킬 사용 불가
3. 점프대 체공 / 넉백: 이동 입력은 제한, 위치 이동은 유지
4. 공중부양 자동 스킬: 직접 이동은 유지하고 지상 장애물 판정 제외
5. 5배 거대화 자동 스킬: 지상 장애물 판정 제외, 몸 충돌과 50% 고정 이동 배율 적용
6. 속도 30% 자동 스킬: 다른 속도 효과보다 우선하는 고정 이동 배율
7. 슬로우: 이동 속도만 감소
8. 질주: 이동 속도 증가, 다른 상태 이상에는 면역 없음

### 수면총의 연속 견제 방향

수면총은 2초 수면 / 2초 쿨타임을 유지한다. 같은 대상이 연속으로 맞는 상황도 의도적으로 허용한다.

- 수면 면역, 수면 시간 감소 같은 자동 억까 방지 장치는 기본값으로 넣지 않는다.
- 한 플레이어가 계속 수면총으로 괴롭히면, 피해자와 다른 플레이어가 기본 총·스킬로 해당 가해자를 함께 견제하거나 처치할 수 있다.
- 밸런스는 피해자 단독 생존률보다, 제3자가 개입했을 때 판이 뒤집히는지로 평가한다.

## 7. 랜덤 스킬 시스템 결정 사항

### 실제 경기 확정안

- 모든 플레이어는 스폰 시 무작위 스킬 1개를 받는다.
- 체크포인트에 들어갈 때마다 보유 스킬은 사라지고, 새 무작위 스킬 1개로 교체된다.
- 직전 보유 스킬은 다음 추첨 후보에서 제외한다.
- 공중부양·속도 30%·5배 거대화는 우클릭을 기다리지 않고 지급 즉시 적용되며 다음 스킬 교체 시 자동 해제된다.
- 스킬은 우클릭으로 사용한다. 좌클릭은 항상 기본 총이다.
- 스킬이 쿨타임 중이어도 체크포인트 재추첨은 가능하다. 새 스킬은 즉시 사용 가능 상태로 시작한다.
- 직전 보유 스킬은 다음 추첨 후보에서 제외하며, 활성 스킬이 하나뿐인 방어적 예외에서만 같은 스킬이 유지된다.
- 모든 추첨은 방장이 선택한 활성 스킬 풀 안에서만 이뤄진다.

## 8. 기본 공격·라이프·협동 견제 시스템

### 의도

스킬만으로는 특정 플레이어가 계속 다른 플레이어를 괴롭히는 상황을 막기 어렵다. 누구나 사용할 수 있는 짧은 사거리의 기본 공격과 라이프를 도입해, 피해를 본 플레이어들이 다른 플레이어와 함께 가해자를 견제하거나 처치할 선택지를 만든다.

이 시스템의 목표는 정교한 슈팅 경쟁이 아니라, **"저 사람 너무 심하다. 같이 잡자"** 같은 병맛 사회적 상호작용을 만드는 것이다.

### 규칙 초안

| 항목 | 제안 |
| --- | --- |
| 조준 | 마우스 방향 |
| 발사 | 좌클릭 |
| 탄창 | 최대 3발 |
| 라이프 | 최대 5칸, 캐릭터 머리 위 픽셀 바 표시 |
| 피해 | 기본 공격 1발당 1칸 |
| 탄창 회복 | 일반 재장전·시간 회복 없음. 구덩이에 빠지거나 체크포인트로 복귀할 때만 3발 전부 회복 |
| 0 라이프 | 마지막 체크포인트에서 부활, 기본 보호 시간 없이 즉시 경기 복귀 |
| 스킬과의 관계 | 수면 중인 대상도 기본 공격 대상이 될 수 있으며, 연속 견제를 허용 |

### 현재 구현 메모

- 좌클릭은 조준 방향으로 기본 총알을 발사하며, 머리 위 탄창 표시에 남은 탄을 표시한다.
- 우클릭은 현재 지급된 스킬 하나만 사용한다.
- 멀티플레이 기본 총은 서버가 탄 수·쿨타임·대상 명중·0 라이프 체크포인트 부활을 판정한다.
- 완주·시간 제한 결과 화면과 최종 순위는 구현됐으며, 처치·피격 통계는 추후 확장한다.

### 밸런스 방향

- 기본 총과 스킬로 특정 플레이어를 집중 견제하거나 리스폰 직후 다시 잡는 상황을 허용한다.
- 자동 피격 무적·리스폰 보호 시간은 기본 규칙으로 넣지 않는다.
- 대신 회복 불가 3발 탄창, 5칸 라이프, 체크포인트 복귀라는 자원 회복 조건으로 한 명이 혼자서 모두를 영구적으로 지배하지 못하게 한다.
- 다수 집중 공격이 발생하면 다른 플레이어가 가해자를 역으로 노릴 수 있도록, 공격자는 3발을 모두 소진하면 구덩이 또는 체크포인트 복귀 전까지 기본 총의 빈틈을 가진다.
- 스킬과 기본 총을 합쳐도 조작이 복잡해지지 않도록, 좌클릭은 항상 기본 총으로 고정한다.

## 9. 멀티플레이 기술 현황과 다음 과제

### 현재 구조

- 프론트엔드: Vite + TypeScript + Canvas (`src/main.ts`)
- 실시간 서버: Node.js + Socket.IO (`server/index.mjs`)
- 공통 규칙: 클라이언트·서버가 `shared/game-rules.mjs`, `shared/geometry.mjs`를 함께 사용
- 실행: `npm run dev` — Vite `127.0.0.1:5174`, Socket.IO 서버 `:5175`
- 서버 주소: 개발 환경은 현재 브라우저 호스트의 `:5175`, 프로덕션은 같은 출처를 사용하며 `VITE_MULTIPLAYER_URL`로 재정의 가능
- 배포: Vite 정적 빌드와 Socket.IO를 단일 Node 프로세스가 같은 포트에서 제공하고 `/healthz`, `/readyz`로 상태를 확인
- DB: 현재 불필요. 방·플레이어·분신은 서버 메모리에만 저장하므로 서버 재시작 시 모두 사라진다.

### 현재 서버 권한

| 영역 | 현재 책임 |
| --- | --- |
| 방 | 서버가 초대 코드, 인원 2~6명, 방장, 시작 가능 여부, 맵·스킬 풀·랩 수·카운트다운·재대결을 관리 |
| 이동 | 서버가 이동 거리 상한과 트랙 경계, 수면/그랩/밀치기 이동 잠금, 분신 충돌을 검증. 돌진·밀치기·분신 최종 위치도 트랙 내부로 제한 |
| 전투 | 서버가 기본 총 탄창·명중·라이프·부활, 수면/슬로우 투사체 충돌, 능동 스킬 7종과 자동 스킬 3종의 효과를 판정 |
| 함정 | 서버가 맵별 구덩이·점프대·회전봉·추락·낙석과 체크포인트 부활을 판정 |
| 표시 | 클라이언트가 약 55ms 단위 상태를 받아 원격 캐릭터를 보간하고 이펙트를 그린다 |
| 레이스 진행 | 서버가 맵별 체크포인트·결승선, 랩 증가·산맥 출발점 순간이동, 체크포인트 탄창 회복·스킬 재지급, 1등 완주·시간 제한 순위를 판정 |

### 구현된 동기화

1. 방 생성·참가·방장 시작·퇴장과 상태 갱신
2. 원격 위치·방향·색상·라이프·탄창·보유 스킬·분신 수 동기화
3. 기본 총, 밀치기, 돌진, 질주, 그랩, 분신, 슬로우탄, 수면총의 전투 이벤트
4. 그랩 팔 왕복/대상 끌기, 밀치기 곡선 이동, 구덩이 낙하, 점프대 체공 이펙트
5. 수면·감속·분신 상태와 슬로우탄 폭발 위치 동기화
6. 서버 기준 체크포인트·랩·1등 완주·함정 상태 동기화
7. 일반 방 생성·참가와 실제 경기 규칙 기반 검증

### 다음 멀티플레이 우선순위

1. 실제 행사 환경과 동일한 장비·네트워크에서 `npm run test:soak` 30분 실행과 수동 2·4·6인 플레이를 확인한다.
2. 세 맵에서 재접속·재대결·함정·능동/자동 스킬 조합을 실제 브라우저 여러 개로 회귀 테스트한다.
3. 실제 5랩 플레이 시간을 측정하고 스킬·함정 밸런스와 시청각 피드백을 조정한다.
4. `src/main.ts`와 `server/index.mjs`의 전투·함정·HUD 경계를 기능 변경 시 점진적으로 추가 분리한다.
5. 초대 코드만으로 부족해질 때 공개 방 목록과 Redis/영속 저장소를 검토한다.

자동 안정화 게이트는 스킬별 재미·수치 밸런스를 평가하지 않는다. 이번 단계에서는 2·4·6인 연결 수용, 상태 패킷 처리, 배포 서버 생존성과 기존 경기 규칙 회귀만 검증한다.

## 10. UI/UX 계획

### 경기 화면 필수 정보

- 왼쪽 상단 레이스 현황판: 모든 플레이어의 현재 랩, 다음 체크포인트, 임시 순위
- 현재 보유한 단일 스킬과 쿨타임
- 머리 위 5칸 라이프 바, 3발 탄창과 구덩이·체크포인트 복귀 시 회복 상태
- 수면/감속/체공 상태 아이콘
- 선두 플레이어 또는 완주 순위

### 피드백 원칙

- 맞은 이유를 한 프레임 안에 알 수 있게 한다.
- 투사체는 색으로 구분한다: 슬로우=청록, 수면=보라.
- 구덩이·점프대·분신에는 충돌 범위와 방향을 읽을 수 있는 픽셀 표식을 둔다.
- 구덩이 표식은 번호 대신 `!`만 표시해 특정 번호보다 위험 자체를 먼저 읽게 한다.
- 게임 이탈은 구형 대기실을 거치지 않고 현재 타이틀 화면으로 돌아간다.

## 11. 아트·사운드 체크리스트

- [x] 플레이어 색상을 반영하는 공통 러너 픽셀 렌더러
- [x] 상태 이상 이펙트: 수면, 감속, 넉백, 질주
- [x] 스킬별 투사체·폭발·분신 이펙트
- [x] 말썽 운동장의 배경·오브젝트·NPC 구성
- [x] 우주 정거장의 금속 패널·우주 공간·행성 배경과 서버 권한 추락
- [x] 우당탕 산맥의 직선 오르막·침엽수·암벽과 서버 권한 낙석
- [x] 짧은 효과음: 발사, 명중, 구덩이, 점프대, 체크포인트, 완주
- [x] Web Audio 64스텝 연속 칩튠 BGM과 결과 징글
- [ ] 외부 제작 캐릭터 프레임 애니메이션과 정식 사운드 마스터링

## 12. 테스트 체크리스트

### 조작감

- [ ] 기본 이동·질주·돌진이 서로 다른 느낌을 주는가
- [ ] 마우스 조준이 화면 크기와 관계없이 정확한가
- [ ] 키보드만으로도 최소한의 플레이가 가능한가
- [ ] 좌클릭 총과 우클릭 스킬이 서로 헷갈리지 않는가
- [ ] 기본 5랩과 방 설정 랩 수에서 레이스 현황판이 정확히 갱신되는가

### 협동 견제와 판 뒤집기

- [ ] 구덩이·수면·넉백의 원인이 즉시 읽히는가
- [ ] 수면총 연속 명중 시 다른 플레이어가 가해자를 견제할 유인이 있는가
- [ ] 분신 20개가 길을 막았을 때도 투사체·스킬·우회로로 판을 뒤집을 수 있는가
- [ ] 선두/후발 모두 스킬을 사용할 이유가 있는가
- [ ] 5 라이프와 회복 불가 3발 탄창이 2인·6인 모두에서 재미있는 다굴·역다굴 상황을 만드는가
- [ ] 여러 플레이어가 협동해 견제하는 상황이 재미있고, 가해자에게도 반격할 수단이 있는가

### 멀티플레이 확인

- [x] `npm run verify`가 자동 테스트를 포함해 전부 통과하는가
- [ ] 일반 방에 브라우저 2개 이상으로 입장해 서로의 이동이 부드럽게 보이는가
- [ ] 방장이 아닌 플레이어도 자신의 고유 의상 색으로 보이는가
- [ ] 기본 총·수면총·슬로우탄·그랩의 대상/명중 결과가 모든 클라이언트에서 같은가
- [ ] 슬로우탄 명중 순간의 폭발과 3.6초 감속 오라가 발사자·피격자·관전자 화면에서 같은가
- [ ] 그랩 발사자와 대상이 로켓팔 왕복 중 이동할 수 없는가
- [ ] 분신이 플레이어만 막고 투사체·그랩 등 스킬은 막지 않는가
- [ ] 분신 생성으로 상대가 겹칠 때 상대가 안전한 방향으로 밀려나는가
- [ ] 2·4·6명에서 20개 분신과 다수 투사체가 프레임 저하를 일으키지 않는가
- [ ] 구덩이 경고 후 같은 구덩이가 모든 화면에서 동시에 열리고, 낙하·리스폰도 같은가
- [ ] 점프대·밀치기의 체공/넉백 이동이 상대 화면에서도 순간이동처럼 보이지 않는가
- [ ] 밀치기로 외곽 또는 내야 트랙 경계를 넘어가 고립되지 않는가
- [ ] 체크포인트·랩·1등 완주가 모든 클라이언트에서 동일하게 확정되는가

## 13. 개발 우선순위

1. **제출 차단 이슈 제거**: 점프대 테스트 실패와 자동 스킬 속도 우선순위 불일치를 해결하고 `npm run verify`를 녹색으로 만든다.
2. **행사 환경 최종 실측**: 행사 장비와 네트워크에서 30분 내구 도구와 수동 2·4·6인 경기를 확인한다.
3. **게임 밸런스 실측**: 세 맵의 5랩 시간, 수면 연속 견제, 거대화 길막, 공중부양 우회, 산맥 낙석 빈도를 조정한다.
4. **정식 에셋·접근성 폴리싱**: 필요할 때 외부 제작 캐릭터 프레임, 사운드 마스터, 키 안내와 가독성 옵션을 보강한다.
5. **운영 확장 판단**: 이용 규모가 늘 때 공개 방 목록, Redis 어댑터와 영속 저장소를 검토한다.

### 향후 확장 가능성

1. **랜덤 경기 이벤트**: 랩 또는 일정 시간마다 초고속·무한 탄창·거대화·미끄러운 트랙 같은 서버 동기화 이벤트
2. **경기 결과 통계와 칭호**: 명중·밀치기·낙하·수면 횟수와 가장 많이 방해한 플레이어 같은 재미 중심 결과
3. **빈자리 AI 러너**: 방장이 빈자리에 서버 제어 봇을 추가하고 실제 참가자가 들어오면 교체
4. **네 번째 맵 후보**: 갈림길·지름길처럼 현재 세 코스와 다른 진행 선택을 가진 신규 코스
5. **방 설정 프리셋**: 기본전·스킬 난장판·구덩이 지옥·초고속 단판과 사용자 설정 저장
6. **공개 방 목록과 운영 확장**: 배포 후 이용자가 늘어날 때 방 검색, 영속 전적과 다중 서버 상태 공유

## 14. 결정이 필요한 질문

### 확정된 결정

- 승리는 설정 랩 수를 가장 먼저 완주한 1명에게만 부여한다.
- 기본 5랩의 목표 플레이 시간은 고정하지 않는다.
- 장기 방에는 1시간 시간 제한을 둔다.
- 분신은 최대 20개이며, 자신을 포함한 플레이어 이동만 막는다.
- 모든 플레이어는 시작 시 랜덤 스킬 1개를 받는다.
- 기본 총은 3발을 모두 사용하면 일반적으로 회복되지 않으며, 구덩이·체크포인트 복귀 시 전부 회복한다.
- 0 라이프는 즉시 체크포인트 부활이다.
- 맵은 방장이 고른다.
- 초기 배포는 단일 Socket.IO 서버의 메모리 방 상태로 운영하며 DB는 넣지 않는다.
- 그랩은 사거리 250의 조준선 명중형이다. 대상이 없어도 로켓팔은 발사·복귀하며, 왕복 중 발사자와 대상은 이동할 수 없다.
- 분신은 플레이어별 최대 20개, 0.15초 생성 간격, 10초 유지다. 플레이어 이동만 막고 스킬은 통과한다.

### 추후 결정

- 제출 빌드에서 멀티플레이가 불안정할 경우 로컬 2~6인 모드를 백업으로 둘지

## 15. 2026-07-24 작업 기록

### 서버 레이스·함정 권한화

- 체크포인트를 순서대로 통과했는지 서버가 직접 검사한다.
- 체크포인트 통과 시 탄창 3발 회복과 활성 스킬 풀 내 랜덤 스킬 재지급을 서버가 처리한다.
- 시작선 복귀 시 랩을 증가시키고 설정 랩을 먼저 완주한 1명만 승자로 확정한다.
- 서버가 랜덤 구덩이의 대상과 개방 시점을 공유한다. 첫 대상은 경기 시작 약 2.5초 뒤 정해지고, 구덩이는 표식 없이 4초 동안 닫힌 상태를 유지한 뒤 열린다.
- 구덩이 낙하·체크포인트 리스폰과 역방향 점프대 체공을 서버 상태로 동기화한다.

### 멀티 전투·애니메이션 안정화

- 수면총과 슬로우탄을 서버 이동 투사체로 바꾸고 선분 충돌로 빠른 투사체의 명중 누락을 줄였다.
- 슬로우탄 명중 좌표를 모든 클라이언트에 보내 청록색 픽셀 폭발과 피격자 지속 오라를 그린다.
- 밀치기는 0.32초 곡선 이동으로 보이며, 서버가 트랙 안의 가장 먼 안전 위치를 찾아 외곽/내야 이탈을 막는다.
- 그랩·밀치기·구덩이·점프대 상태 중에는 서버와 클라이언트가 동일한 이동 잠금과 최종 위치를 사용한다.
- 구덩이 위 경고 표식은 제거하고 화면 알림만 유지한다.

### 화면 흐름 정리

- 인게임의 `로비로 돌아가기` 동작을 `메인 화면으로 돌아가기`로 정리했다.
- 복귀 시 멀티 방, 원격 플레이어, 분신, 투사체, 상태 이상, 열려 있던 방 설정 패널을 초기화한다.
- 구형 `MALSSUNG LOBBY` 화면은 복귀 경로에서 숨기고 현재 픽셀 타이틀 화면을 표시한다.

### 이번 작업 검증

- `node --check server/index.mjs`
- `npm run check`
- `npm run build`
- 2클라이언트 Socket.IO 통합 점검: 트랙 오른쪽 끝 밀치기 도착점 `x=1276`, 다음 구덩이 경고 약 `4033ms`, 슬로우탄 대상 1명과 명중 좌표 수신 확인
- 로컬 브라우저 점검: 일반 멀티플레이 방에서 게임 이탈 후 타이틀 표시·구형 대기실 숨김·방 패널 닫힘 확인

## 16. 새 세션 인수인계

### 바로 확인할 파일

- `plan.md`: 제품 규칙·현재 구현·남은 우선순위
- `src/main.ts`: Canvas 렌더링, 입력, Socket.IO 클라이언트
- `server/index.mjs`: 방 상태, 이동 검증, 전투/분신/그랩 서버 판정
- `shared/game-rules.mjs`: 스킬·속도·상태·공통 경기 규칙
- `shared/map-catalog.mjs`: 운동장·우주 정거장·산맥의 코스와 함정 데이터
- `shared/map-hazards.mjs`, `shared/map-obstacles.mjs`: 루프 맵 무작위 함정·장애물 생성
- `src/style.css`: 타이틀·게임 HUD·월드 라벨·쿨타임 스피너
- `package.json`: 개발·빌드·전체 검증·부하/내구 테스트 명령

### 로컬 실행과 테스트

```bash
npm install
npm run dev
```

- 게임: `http://127.0.0.1:5174`
- 일반 방: 방 생성 후 다른 브라우저/시크릿 창에서 초대 코드로 참가, 방장이 시작
- 확인 명령: `npm run verify`
- 부하 검증: `npm run test:load`
- 30분 내구 검증: `npm run test:soak`

### 최근 구현 커밋

- `8df576f`: 자동 스킬 3종과 맵 3종 기획 문서화
- `04abbbf`: 우주 정거장·우당탕 산맥과 자동 스킬 서버/클라이언트 통합
- `13cdc19`: 루프 맵 무작위 함정·장애물 생성 규칙
- `de3b1d8`: 맵별 연속 칩튠 음악 테마
- `c4191ef`: 멀티플레이 장시간 내구·메모리 점검
- `6f1e861`: 인게임 이탈 시 현재 타이틀 화면 복귀
- `86bbd27`: 서버 체크포인트·랩·함정·수면/슬로우 투사체 권한화
- `01a4e9a`: 클라이언트 레이스·함정·넉백·슬로우 피드백 동기화

## 17. 2026-07-24 기술 부채 정리

- 트랙·체크포인트·함정·스킬 한도 등 중복 상수를 `shared/game-rules.mjs`로 통합했다.
- 트랙 판정과 선분 거리를 `shared/geometry.mjs`, 이동 속도·경계 검증을 `shared/movement-validation.mjs`로 분리했다.
- 클라이언트의 게임·네트워크 타입을 `src/game/types.ts`로 분리했다.
- 서버가 일반 이동의 트랙 이탈과 경계를 넘는 돌진·분신 배치를 거부하도록 보강했다.
- 회전봉 위상을 서버 경기 시간과 동기화하고, 충돌·넉백을 서버 판정으로 전환했다.
- 방 참가 화면에도 러너 이름 입력을 노출했다.
- 사설 Nexus 주소가 들어 있던 잠금 파일을 공식 npm 레지스트리 기준으로 재생성했다.
- 취약한 개발용 `concurrently` 의존성을 제거하고 `scripts/dev.mjs`로 대체했다.
- Node 내장 테스트를 추가하고 `npm test`, `npm run verify` 명령을 제공한다.

## 18. 2026-07-25 경기 흐름 완결

- 서버 방 상태를 `waiting → running → finished` 단계로 명시하고 경기 회차를 기록한다.
- 최초 완주 시 서버가 모든 참가자의 진행도와 다음 관문 거리를 기준으로 전체 최종 순위를 확정한다.
- 경기가 1시간을 넘으면 같은 기준으로 현재 선두와 전체 순위를 확정한다.
- 결과 화면에 순위, 진행도, 경기 시간과 방 코드를 표시한다.
- 방장만 같은 참가자와 설정을 유지한 채 재대결을 시작할 수 있고, 모든 플레이어 상태와 함정을 새 회차 기준으로 초기화한다.
- 2클라이언트 통합 테스트로 시간 제한 종료, 동일 결과 수신, 비방장 권한 거부, 재대결 초기화를 검증한다.

## 19. 2026-07-25 멀티플레이 상태 충돌 안정화

- `falling → airborne → grappled → pushed → sleeping → normal` 우선순위를 `shared/player-state.mjs`에 정의했다.
- 이동·기본 공격·스킬·피격·강제이동이 같은 서버 상태 규칙을 사용한다.
- 구덩이는 모든 시간제 상태를 정리하고, 점프는 그랩·밀치기·수면을 취소하며, 밀치기와 그랩은 동시에 중첩되지 않는다.
- 수면 중 밀치기처럼 허용된 조합은 밀치기가 끝난 뒤 남은 수면 시간이 이어지도록 처리한다.
- 점프대 도착 좌표를 서버가 트랙 안의 안전 위치로 확정하고 체공 중 이동 패킷을 거부한다.
- 상태 잔여 시간을 서버 스냅샷에 포함해 수면·감속·질주·그랩·밀치기를 클라이언트와 재동기화한다.
- 단위 테스트와 실제 Socket.IO 테스트로 상태 우선순위, 점프 잠금, 재대결 초기화, 2·4·6인 이동 전파를 검증한다.

## 20. 2026-07-25 경기 시작 카운트다운

- 방장이 경기 시작 또는 재대결을 요청하면 서버가 3초 카운트다운 종료 시점을 확정한다.
- 모든 참가자는 서버가 전달한 남은 시간을 기준으로 `3 → 2 → 1 → START` 오버레이를 표시한다.
- 카운트다운 동안 방은 아직 `running`으로 전환되지 않으므로 이동·공격·스킬 이벤트가 서버에서 거부된다.
- 카운트다운 도중 참가자가 2명 미만으로 줄면 출발을 취소하고 기존 대기실 또는 결과 화면을 유지한다.
- 카운트다운이 끝난 시점부터 경기 시간과 함정 주기를 계산하고 새 회차의 플레이어 상태를 초기화한다.
- Socket.IO 통합 테스트에서 카운트다운 상태, 입력 잠금, 시작 이벤트, 재대결 및 2·4·6인 출발 동기화를 검증한다.

## 21. 2026-07-25 재접속과 경기 상태 복구

- 서버가 `socket.id`와 독립된 고정 플레이어 UUID와 256비트 재접속 토큰을 발급한다.
- 브라우저는 방 코드·플레이어 ID·재접속 토큰을 탭의 세션 저장소에 보관하고 새 소켓 연결 시 기존 방을 자동 복구한다.
- 예기치 않은 연결 종료 후 30초 동안 위치·랩·체크포인트·체력·탄약·스킬과 시간제 상태를 서버 메모리에 유지한다.
- 재접속 시 새 `socket.id`를 기존 플레이어에 연결하고 서버 스냅샷으로 대기실·경기·결과 화면을 복원한다.
- 연결이 끊긴 플레이어는 공격과 함정 대상에서 제외되며 다른 참가자 화면에는 재접속 중으로 표시한다.
- 방장은 유예 시간 동안 권한을 유지하고, 만료되면 연결된 다음 참가자에게 방장 권한을 위임한다.
- Socket.IO 통합 테스트에서 토큰 비공개, 상태 효과·위치·자원 복구, 방장 재접속, 유예 만료와 잘못된 세션 거부를 검증한다.

## 22. 2026-07-25 모듈화·부하·배포 안정화

- 서버 실행 설정을 `server/config.mjs`, 정적 SPA와 상태 확인 처리를 `server/http-handler.mjs`, 고정 플레이어 세션 규칙을 `server/player-session.mjs`로 분리했다.
- 클라이언트의 세션 저장·같은 출처 배포 주소 선택을 `src/game/network-session.ts`, 카운트다운 시간 계산을 `src/game/match-countdown.ts`로 분리했다.
- 프로덕션에서는 Vite 빌드와 Socket.IO를 Node 서버 하나가 같은 포트에서 제공한다. 정적 파일 경로 이탈을 차단하고 해시 자산은 장기 캐시, SPA 문서는 재검증하도록 설정했다.
- `/healthz`는 방·전체/연결 플레이어·소켓·경기 단계 수를, `/readyz`는 정적 빌드 준비 여부를 반환한다.
- `SIGINT`, `SIGTERM`과 테스트용 IPC 종료 요청에서 타이머·Socket.IO·HTTP 서버를 순서대로 닫는 정상 종료 경로를 추가했다.
- 부하 테스트는 2·4·6인 방을 동시에 열어 12개 소켓으로 1,080개의 위치 상태를 전송하고, 최소 2,000개의 원격 전파와 세 방의 생존·연결 상태를 확인한다.
- 배포 테스트는 정적 문서·해시 자산 캐시·SPA fallback·누락 자산·경로 이탈·readiness·Socket.IO 연결·정상 종료를 검증한다.
- 스킬의 재미나 수치 밸런스는 자동 검증 범위에서 제외하고 기존 기능 회귀 테스트만 유지한다.

## 23. 2026-07-25 장시간 검증·구조 분리·콘텐츠 확장

- `shared/map-catalog.mjs`가 말썽 운동장의 이름, 색상과 서버 권한 구덩이·점프대·회전 장애물 배치를 공유한다.
- 동일한 코스를 재활용한 공사장·옥상 프로토타입과 방 생성 맵 선택 UI를 제거했다.
- 러너 픽셀 렌더러는 외곽선, 유니폼 명암, 얼굴 방향과 보행 프레임을 한 모듈에서 그려 로컬·원격·NPC가 같은 캐릭터 규칙을 사용한다.
- 키보드·포인터 입력, Web Audio, 맵 콘텐츠, 월드 렌더링, 러너 렌더링을 `src/game/` 모듈로 분리했다.
- 서버의 방 코드·이름·맵·인원·랩·스킬 설정 검증과 방 초기 상태 생성을 `server/room-state.mjs`로 분리했다.
- 기본 총·빈 탄창·스킬·피격·체크포인트·카운트다운·구덩이·점프·완주 효과음과 64스텝 연속 칩튠 BGM을 Web Audio로 생성하고 음소거 설정을 보관한다.
- `/healthz`에 RSS·힙 사용량을 포함하고 `npm run test:soak`가 기본 30분 동안 2·4·6인 세 방, 12개 소켓, 상태 전파와 중간 재접속을 검증한다.
- 로컬 2분 내구 실행에서 상태 22,968건 전송, 원격 전파 84,211건, 재접속 1회, 최대 상태 응답 11.6ms, 최대 RSS 66.7MB, 최대 힙 15.4MB를 기록했다.
- 실제 행사 장비·공유기 환경은 이 작업 공간에서 재현할 수 없으므로 같은 30분 명령과 수동 경기 체크를 현장에서 마지막으로 수행한다.

## 24. 2026-07-25 경기 화면·사운드·맵 정리

- 1.6초 안팎으로 끊겨 들리던 짧은 선율 반복을 64스텝 멜로디·화음·베이스·리듬을 미리 예약하는 연속 BGM으로 교체했다.
- 전체화면과 소리 버튼을 하나의 flex 그룹으로 묶어 `전체 화면 종료` 문구에서도 서로 겹치지 않게 했다.
- 우측 상단 LAP·OBJECTIVE HUD와 우측 PLAYER·조작법 패널을 DOM·스타일·갱신 코드까지 제거하고, 필요한 이동 버튼만 우측 하단에 남겼다.
- 구덩이가 닫힌 뒤 다시 열리기 전의 시간을 0.9초에서 4초로 늘리고 구덩이 위 경고 표식을 제거했다.
- 같은 코스에 테마만 달랐던 공사장·옥상 맵과 선택 UI·렌더링·테스트 데이터를 제거해 말썽 운동장만 유지한다.

## 25. 2026-07-25 BGM·수면 모션·원격 라벨 보완

- 연속 BGM 멜로디 사이에 배치했던 짧은 노이즈 하이햇 두 종류를 제거해 따닥거리는 타악음 없이 멜로디·화음·베이스만 이어지게 했다.
- 수면총에 맞은 로컬 플레이어와 원격 플레이어가 옆으로 누운 공통 수면 프레임과 머리 위로 떠오르는 `Z`를 사용한다. 확대 화면에서 전신 1픽셀 호흡 이동이 깜빡임처럼 보여 몸은 고정했다.
- 연결이 끊긴 원격 플레이어는 재접속 유예 동안 캐릭터와 월드 이름·스킬 라벨을 렌더링하지 않으며, 완전히 퇴장하면 별도 라벨 DOM도 즉시 제거한다.

## 26. 2026-07-25 방 코드 공유와 경기 메뉴 정리

- 레이스 우측 하단의 별도 스킬 시험 화면 이동 버튼과 관련 전환 코드를 제거하고 `메인 화면으로 돌아가기`만 남겼다.
- 개발 전용 방 진입은 제출 빌드의 일반 사용자 흐름과 분리하기 위해 제거했다.
- 방 생성 후 대기 화면에서 방장에게만 현재 방 코드를 크게 표시하고 한 번에 복사할 수 있는 버튼을 추가했다.
- 보안 컨텍스트에서는 Clipboard API를 사용하고, 지원되지 않는 환경에서는 숨김 입력 선택 방식으로 복사를 시도한다.
- 방장이 바뀌면 새 방장에게 공유 영역이 자동으로 표시되고 일반 참가자에게는 숨겨진다.

## 27. 2026-07-25 연속 중복 스킬 방지

- 체크포인트와 새 경기 시작 시 스킬을 다시 뽑을 때 직전에 보유한 스킬을 후보에서 제외한다.
- 클라이언트와 멀티플레이 서버가 `pickNextSkill` 공통 규칙을 사용해 같은 방식으로 동작한다.
- 실제 방은 스킬을 최소 3개 선택하므로 스킬 갱신 때마다 반드시 다른 스킬이 지급된다.
- 단일 후보나 빈 목록 같은 방어적 예외 처리와 여러 난수 위치에서 이전 스킬이 나오지 않는지를 단위 테스트로 검증한다.

## 28. 2026-07-25 자동 발동 스킬 확장

- `공중부양`은 지급 즉시 캐릭터를 떠 있는 모습으로 표시하며 이동과 기본 총은 유지하고 서버·로컬 지상 장애물 판정을 건너뛴다.
- `속도 30%`는 지급 즉시 기본 이동 속도의 30%로 고정되며 질주나 슬로우와 중첩되지 않는다.
- `5배 거대화`는 지급 즉시 캐릭터와 몸 충돌을 5배로 키우고 이동 속도를 50%로 고정하며 지상 장애물과 분신을 무시하고 체력 피해를 받지 않는다.
- 거대화된 플레이어가 길을 막으면 다른 플레이어는 통과할 수 없고, 이미 몸이 겹친 경우에는 서로 멀어지는 이동만 허용한다.
- 거대 몸 충돌은 중심 기준 대칭 범위가 아니라 실제 픽셀 스프라이트의 위·아래·좌·우 외곽에 맞춰 빈 공간을 막지 않는다.
- 세 스킬은 우클릭으로 다시 발동하지 않고 다음 체크포인트에서 다른 스킬을 받을 때 자동으로 해제된다.
- 방 생성 스킬 목록, 공통 스킬 타입, 서버 설정 검증, 원격 캐릭터 표현과 재접속 상태 복구에 세 스킬을 포함한다.

## 29. 2026-07-26 우주 정거장 맵

- 방 생성 화면에서 말썽 운동장과 우주 정거장을 직접 선택할 수 있다.
- 우주 정거장은 금속 패널 트랙, 별빛 우주 공간, 내야의 픽셀 행성, 경고 스트라이프로 운동장과 구분한다.
- 운동장은 기존처럼 트랙 경계에서 이동을 차단하고, 우주 정거장은 경계를 넘는 이동·밀치기·그랩을 허용한 뒤 서버가 추락을 확정한다.
- 우주 공간 추락은 낙하 애니메이션 후 체력·탄약을 복구하고 마지막 체크포인트에서 부활한다.
- 추락 애니메이션은 고정된 아래쪽이 아니라 실제 이탈 방향으로 이어져, 상단 이탈 시 트랙 위로 되돌아오지 않고 위쪽 우주 공간으로 떨어진다.
- 공중부양은 우주 공간을 건널 수 있지만 거대화와 나머지 스킬은 추락한다.
- 맵 카탈로그·이동 검증·서버 추락 이벤트·부활 상태를 단위 및 실제 Socket.IO 통합 테스트로 검증한다.

## 30. 2026-07-26 우당탕 산맥 맵

- 방 생성 화면에 운동장·우주 정거장과 별개의 `우당탕 산맥`을 추가했다.
- 산 아래에서 정상까지 이어지는 완만한 직선 오르막에 기존과 동일한 체크포인트 3개를 배치했다.
- 정상 도착으로 한 랩을 확정하고 남은 랩이 있으면 서버가 플레이어를 산 아래 출발점으로 순간이동시킨다.
- 기존 구덩이·점프대·회전봉은 제거하고, 서버가 정상에서 아래로 굴리는 낙석과 이를 소멸시키는 암벽 구조물을 배치했다.
- 낙석 위치는 서버 스냅샷과 생성·소멸 이벤트로 동기화하며, 공중부양과 거대화는 지상 낙석 피해를 받지 않는다.
- 맵별 이동 경계·체크포인트·스폰·결승선을 공통 맵 카탈로그 데이터로 분리했다.
- 직선 트랙 이동, 1랩 순간이동, 최초 낙석의 암벽 충돌을 단위 및 실제 Socket.IO 통합 테스트로 검증했다.

## 31. 2026-07-27 저장소 점검과 제출 전 잔여 작업

### 점검 결과

- 마지막 정리 기준점 이후 추가된 27개 커밋과 48개 변경 파일을 확인했다.
- 경기 종료·결과·재대결, 3초 카운트다운, 30초 재접속 유예, 3개 맵, 액티브 7종·패시브 3종 스킬, 배포·부하·내구 검증 코드가 현재 저장소에 반영되어 있다.
- 점프대 서버 판정은 정상이며, 무작위 회전 장애물과 통합 테스트의 고정 이동 경로가 간섭하던 원인을 확인해 안전 경로를 사용하도록 수정했다.
- `속도 30%`와 `5배 거대화`는 서버와 클라이언트가 동일한 공통 이동 배율 계산을 사용해 질주·슬로우가 중첩되지 않는다.
- 산맥 완주 테스트는 낙석 검증 중 테스트 플레이어가 낙석에 먼저 맞지 않도록 결정적인 자동 스킬 순서를 사용한다.
- 타입 검사, 자동 테스트 35개, 프로덕션 빌드와 서버 구문 검사를 포함한 `npm run verify`가 모두 통과한다.

### 완료된 P0 안정화

1. 점프대 통합 테스트 실패 원인 격리와 안전 이동 경로 적용
2. `속도 30%`·질주·슬로우 우선순위의 서버·클라이언트 통일
3. `npm run verify`의 타입 검사·35개 테스트·빌드·서버 검사 전체 통과

### P1 — 행사 환경 검증

1. 실제 사용할 PC와 공유기에서 2·4·6인 경기를 각각 실행한다.
2. 운동장·우주 정거장·산맥의 함정, 완주, 재대결을 브라우저 여러 대에서 확인한다.
3. 돌진·밀치기·그랩·수면·슬로우·분신과 패시브 스킬 조합을 상호 피격 중심으로 확인한다.
4. 경기 중 새로고침·일시 단절·30초 내 재접속과 방장 이관을 확인한다.
5. `npm run test:soak`를 최소 30분 실행하고 지연, 메모리, 연결 끊김 여부를 기록한다.

### P2 — 게임성·표현 보완

1. 5랩 기준 실제 플레이 시간을 측정하고 맵별 장애물 주기와 스킬 수치를 조정한다.
2. 밀치기·그랩·수면처럼 체감 차이가 큰 스킬의 사거리, 넉백, 쿨타임을 플레이테스트로 조정한다.
3. 맵별 효과음·BGM 음량과 피격·낙하·완주 피드백을 정리한다.
4. 작은 화면의 HUD 가독성, 색상 구분, 키보드 포커스와 음소거 상태를 점검한다.

### P3 — 제출 이후 확장

1. `src/main.ts`와 `server/index.mjs`의 남은 경기 상태·네트워크·렌더링 책임을 추가 모듈로 분리한다.
2. 공개 방 목록, 관전자, 강제 퇴장 같은 운영 기능을 검토한다.
3. 여러 서버 인스턴스가 필요해질 때 Redis 어댑터와 외부 세션·방 저장소를 도입한다.
4. 전적·통계·신고 기록이 필요할 때 영속 데이터베이스를 추가한다.

## 32. 2026-07-27 산맥 코스 길이·속도·낙석 제어 보완

- 낙석에 맞아 납작해진 플레이어는 애니메이션과 서버 부활 처리가 끝날 때까지 이동 입력이 잠긴다.
- 서버는 기존 공통 행동 상태 정책으로 납작 상태의 이동 패킷을 거부하고, 클라이언트도 로컬 예측 이동을 즉시 중단한다.
- 산맥의 일반 보행 속도는 다른 맵의 85%로 적용하며, 서버 이동 검증과 클라이언트 이동 계산이 같은 맵 배율을 사용한다.
- 돌진·밀치기·그랩·낙석처럼 별도 변위를 사용하는 효과에는 산맥 보행 배율을 적용하지 않는다.
- 산맥 중심 경로를 약 `2000.66 → 2400.80`으로 늘려 기존보다 약 20% 긴 오르막으로 확장했다.
- 출발 지점과 트랙 폭은 유지하고, 정상·결승선·체크포인트·부활 지점·암벽을 진행 방향으로 재배치했다.
- 낙석 중 이동 잠금, 연장된 코스 완주, 공중부양 체크포인트와 낙석 부활을 통합 테스트로 검증했다.
- 타입 검사, 자동 테스트 35개, 프로덕션 빌드와 서버 검사를 포함한 `npm run verify`가 모두 통과한다.

## 33. 2026-07-27 UI/UX 폴리싱과 개발 전용 진입 제거

- 타이틀의 참여·생성 버튼을 키우고 배경 대비와 상호작용 피드백을 보강해 첫 행동을 더 빠르게 구분할 수 있게 했다.
- 방 생성 패널의 폭과 정보 밀도를 조정하고 주요 버튼을 스크롤 하단에 고정해, 낮은 화면에서도 `방 열기`와 `뒤로`가 사라지지 않게 했다.
- 온라인 대기실에 현재 맵, 랩 수, 활성 스킬 수, 참가 인원, 방장·연결 상태를 표시하는 러너 목록을 추가했다.
- 인게임 레이스 보드의 크기와 행 간격을 보강하고 전체 화면·음소거 버튼은 우측 상단, 조작 안내와 게임 나가기는 하단의 서로 다른 영역으로 정리했다.
- 키보드 포커스 외곽선을 통일하고 운영체제의 동작 줄이기 설정에서는 로고·쿨타임·대기 상태 애니메이션을 정지한다.
- 참가 화면에서 개발 전용 코드를 안내하거나 특별 처리하던 분기를 제거했다. 모든 초대 코드는 실제 생성된 대기 중 방에만 참가할 수 있다.
- 로컬 연습 화면을 여는 입력 분기와 전용 키보드 조작, 특별 테스트 방을 자동 생성하던 서버 로직과 관련 통합 테스트를 제거했다.
- 브라우저에서 타이틀, 방 생성, 온라인 대기실, 2인 인게임 HUD를 확인했으며 타입 검사, 자동 테스트 35개, 프로덕션 빌드와 서버 검사를 포함한 `npm run verify`가 통과한다.

## 34. 2026-07-27 기술 부채 정리와 운영 방어선

- 진입 경로가 제거된 연습장 타입·충돌·테스트 봇 AI·로컬 전투·렌더링 코드를 완전히 삭제했다.
- 월드 장애물 렌더링을 `src/game/world-prop-renderer.ts`, 방 스냅샷 직렬화를 `server/room-snapshot.mjs`로 분리했다.
- Socket.IO 이벤트 이름, 요청·응답 DTO와 런타임 입력 파서를 `shared/network-protocol.mjs`와 선언 파일로 통합했다.
- 클라이언트 Socket.IO 연결에 공통 이벤트 타입을 적용하고, 서버 JavaScript도 `tsconfig.server.json`의 `checkJs` 검사를 받는다.
- 인메모리 방 저장소를 `server/room-store.mjs` 경계로 분리해 향후 Redis 어댑터로 교체할 수 있게 했다.
- 전체 방 수, 주소별 연결 수, 방·상태·전투 이벤트 속도 제한과 구조화 서버 로그를 추가했다.
- 운영 환경에서 허용 출처를 지정하지 않으면 교차 출처 Socket.IO 접근을 허용하지 않으며, 개발 환경에서는 기존 로컬 흐름을 유지한다.
- 실제 Chromium 두 개로 방 생성·참가·카운트다운·Canvas 렌더링·HUD 배치·입력·연결 종료 라벨을 검증하는 Playwright 테스트를 추가했다.
- 타이틀 키비주얼을 약 1.99MB PNG에서 약 308KB WebP로 최적화하고 사용하지 않는 이전 시안들을 제거했다.
- Vite 8.1.5, TypeScript 7.0.2와 Playwright 1.62.0으로 개발 도구를 갱신했다.
